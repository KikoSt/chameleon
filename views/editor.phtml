<div id="awesomeEditor" class="row" style="margin-top: 70px; padding-left: 10px; padding-right: 10px;">
    <form enctype="multipart/form-data" id="editor" method="POST">
    <div class="col-md-5">
        <div class="panel-group" id="accordion">
                <?php
                    foreach($this->elements as $element):
                        $type = get_class($element);
                            include($type . '.inc.php');
                    endforeach;
                ?>
            <input type="hidden" name="templateId" value="<?php echo $this->templateId; ?>">
            <input type="hidden" name="advertiserId" value="<?php echo $this->advertiserId; ?>">
            <input type="hidden" name="companyId" value="<?php echo $this->companyId; ?>">
        </div>
    </div>
    </form>
    <div class="col-md-7">
        <div class="row" style="margin-left: 5px;">
            <img src="<?php echo $this->gif;?>" alt="">
        </div>
        <div class="row" style="padding-top: 5px; margin-left: 5px;">

            <!--        On click render template and show the change-->
            <button id="preview" type="submit" class="btn btn-info" form="editor">Preview</button>
            <!--        On click render template, show the change and save data to database-->
            <button id="save" type="submit" class="btn btn-danger" form="editor">Save</button>

            <div id="previewalert" class="alert alert-info alert-dismissible" role="alert">
                <button type="button" class="close" data-dismiss="alert">
                    <span aria-hidden="true">&times;</span>
                    <span class="sr-only">Close</span>
                </button>
                Preview successfully created...
            </div>
        </div>
    </div>
</div>


<script type="text/javascript">
    $(document).ready(function() {
        var btn;

        $('.btn').on('click', function(){
            btn = $(this).attr('id');
        });

        $('#previewalert').hide();

        $(window).keydown(function(e){
            if(e.keyCode == 13) {
                e.preventDefault();
                return false;
            }
        });

        $('#editor').on('submit', function(e){
            e.preventDefault();

            var nodeList = document.querySelectorAll('.file');
            var formData = new FormData();



            for (var i = 0, length = nodeList.length; i < length; i++)
            {
                var myId = nodeList[i].getAttribute('id');

                var fileSelect = document.querySelector("#" + myId);
                var files = fileSelect.files;

                console.log(files);
            }



            $.ajax({
                type: 'POST',
                url: '/chameleon/ajax/changeSvg.php',
                data: $('#editor').serialize() + "&action=" + btn,
                success: function(){
                    $('#previewalert').show();
                    location.reload();
                }
            });
        });

        $('.picker').colorpicker();

        $("#fileUpload").fileinput();
    });
</script>

