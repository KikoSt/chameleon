    <?php
        if(!is_array($this->previews)):
    ?>
            <h1>An error occurred. Please try reloading the page ...</h1>
    <?php
        else:
    ?>
    <div style="margin-top: 70px; padding-left: 10px;">
            <h2 class="text-center">Select the banner to be edited</h2>
    <?php
        $count = count($this->previews);
        $i = 0;
        foreach($this->previews as $preview):
    ?>
                <div class="col-md-2" >
                <form method="post" action="index.php?page=editor" >
                    <input type="image" src="<?php echo $preview->filepath; ?>" alt="" width="<?php echo $preview->width; ?>" onclick="form
                    .submit
                    ();">
                    <input type="hidden" name="id" value="<?php echo $preview->id; ?>">
                    <input type="hidden" name="advertiserId" value="<?php echo $preview->advertiserId; ?>">
                    <input type="hidden" name="companyId" value="<?php echo $preview->companyId; ?>">
                    <input type="hidden" name="templateName" value="<?php echo $preview->templateName; ?>">
                </form>
                </div>


    <?php
        endforeach;
        endif;
    ?>
    </div>





